function ktDataTableOrderByOrderColumn (tableData) {
    return $(tableData).sort(function (a, b) {
        if ( a.Order < b.Order ){
            return -1;
        }
        if ( a.Order > b.Order ){
            return 1;
        }
        return 0;
    });
}